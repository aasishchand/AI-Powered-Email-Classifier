# API base URL (include /api/v1)
# Must include /api/v1 (FastAPI prefix). Plain http://localhost:8000 will break pipeline & other routes.
VITE_API_URL=http://localhost:8000/api/v1
# WebSocket base URL (optional)
VITE_WS_URL=http://localhost:8000
